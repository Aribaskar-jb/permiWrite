import { StyleSheet, View, Text, TextInput, TouchableOpacity, Button } from 'react-native';

export default function Letter() {

    return(
        <View style={styles.overall}>
            <Text>Hello, Fill in your letter!</Text>

        </View>
    )
}

const styles = StyleSheet.create({

})
